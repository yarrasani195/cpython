apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName:  new-persistent
spec:
  entrypoint: welcome persistent 

  # This spec contains one templates: welcome persistent and whalesay
  templates:
  - name: welcome persistent
    # Instead of just running a container
    # This template has a sequence of steps
    steps:
    - - name: welcome persistent         
        template: whalesay
        arguments:
          parameters:
          - name: welcome persistent
            value: "welcome persistent"